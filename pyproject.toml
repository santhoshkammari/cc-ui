[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ccui"
version = "0.2.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "claude-agent-sdk",
]

[project.scripts]
ccui = "ccui.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["ccui"]
