[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ccui"
version = "0.1.0"
dependencies = [
    "gradio",
    "claude-agent-sdk",
]

[project.scripts]
ccui = "ccui.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["ccui"]
